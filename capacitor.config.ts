import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.handien.tudien",
  appName: "Hán Điển",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
